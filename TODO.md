- [x] Update plan confirmation (needed to include current user outside top-100)
- [x] Modify `app/api/leaderboard/route.ts` to accept optional `code` query param and always include that user
- [x] Update `app/leaderboard/page.tsx` to call `/api/leaderboard?code=${resolvedCode}`
- [x] Recompute ranks after merging top 100 + current user
- [ ] Validate manually: user with 250 coins appears immediately and is marked YOU
